import { useMemo, useState, type ElementType, type FormEvent, type ReactNode } from "react";
import { useLocation } from "wouter";
import { toast } from "sonner";
import { MapView } from "@/components/Map";
import {
  Activity, ArrowDownRight, ArrowUpRight, Bell, Bot, BriefcaseBusiness, Check, ChevronDown,
  ChevronRight, CircleHelp, Clock3, Copy, Database, FileCode2, FileText, Filter, Gauge,
  KanbanSquare, KeyRound, LayoutDashboard, ListFilter, Moon, MoreHorizontal, PanelLeftClose,
  Plus, RefreshCw, Search, Send, Settings2, Sparkles, Sun, Target, TerminalSquare, Users, X,
} from "lucide-react";

type Stage = "New" | "Contacted" | "Interested" | "Proposal" | "Won" | "Lost";
type Lead = { id: string; business: string; niche: string; location: string; score?: number; stage: Stage; contact: string; lastTouch: string; value: number; initials: string; address?: string; website?: string; placeId?: string };

const stages: Stage[] = ["New", "Contacted", "Interested", "Proposal", "Won", "Lost"];
const navSections = [
  { label: "Workspace", items: [{ label: "Dashboard", href: "/", icon: LayoutDashboard }, { label: "Find leads", href: "/find-leads", icon: Search }, { label: "Add leads", href: "/add-leads", icon: Plus }, { label: "Pipeline", href: "/pipeline", icon: KanbanSquare }] },
  { label: "Operations", items: [{ label: "AI tools", href: "/ai-tools", icon: Bot }, { label: "Revenue", href: "/revenue", icon: BriefcaseBusiness }, { label: "Logs", href: "/logs", icon: TerminalSquare }] },
  { label: "System", items: [{ label: "Settings", href: "/settings", icon: Settings2 }, { label: "Design system", href: "/design-system", icon: Sparkles }] },
];

function cn(...classes: Array<string | false | null | undefined>) { return classes.filter(Boolean).join(" "); }
const STORAGE_KEY = "jarvis-leads";
function getSavedLeads(): Lead[] { if (typeof window === "undefined") return []; try { return JSON.parse(window.localStorage.getItem(STORAGE_KEY) ?? "[]") as Lead[]; } catch { return []; } }
function saveLead(lead: Lead) { const next = [...getSavedLeads().filter((item) => item.id !== lead.id), lead]; window.localStorage.setItem(STORAGE_KEY, JSON.stringify(next)); return next; }
function formatCurrency(value: number) { return new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR", maximumFractionDigits: 0 }).format(value); }
function stageTone(stage: Stage) { return { New: "neutral", Contacted: "blue", Interested: "amber", Proposal: "violet", Won: "green", Lost: "red" }[stage]; }
function StatusBadge({ stage }: { stage: Stage }) { return <span className={cn("badge", `badge-${stageTone(stage)}`)}>{stage}</span>; }

function Shell({ children, onThemeToggle, light }: { children: ReactNode; onThemeToggle: () => void; light: boolean }) {
  const [location, setLocation] = useLocation();
  const current = location === "/" ? "/" : `/${location.split("/")[1]}`;
  const [collapsed, setCollapsed] = useState(false);
  const [commandOpen, setCommandOpen] = useState(false);
  const [searchTerm, setSearchTerm] = useState("");
  return <div className={cn("app-shell", collapsed && "sidebar-collapsed")}>
    <aside className="sidebar"><div className="sidebar-top"><button className="brand" onClick={() => setLocation("/")}><span className="brand-mark">J</span><span className="brand-name">jarvis</span></button><button className="icon-button sidebar-collapse" onClick={() => setCollapsed((v) => !v)}><PanelLeftClose size={16} /></button></div>
      <div className="workspace-switcher"><span className="workspace-avatar">M</span><span className="workspace-copy"><strong>Meridian Studio</strong><small>Hyderabad, India</small></span><ChevronDown size={14} className="muted-icon" /></div>
      <nav className="nav-groups">{navSections.map((section) => <div className="nav-group" key={section.label}><div className="nav-section-label">{section.label}</div>{section.items.map((item) => { const Icon = item.icon; return <button key={item.href} className={cn("nav-item", current === item.href && "nav-item-active")} onClick={() => setLocation(item.href)}><Icon size={16} strokeWidth={1.7} /><span>{item.label}</span></button>; })}</div>)}</nav>
      <div className="sidebar-bottom"><div className="live-stat"><span className="live-dot" /><span>System online</span><span className="stat-value mono">100%</span></div><div className="sidebar-stat"><span>Leads this month</span><strong>0</strong></div><div className="sidebar-stat"><span>Last sync</span><strong className="mono">Not synced</strong></div><button className="profile-row"><span className="profile-avatar">MC</span><span className="profile-copy"><strong>Marin Cole</strong><small>Owner</small></span><MoreHorizontal size={15} className="muted-icon" /></button></div>
    </aside>
    <main className="main-area"><header className="topbar"><div className="breadcrumbs"><span>Meridian Studio</span><ChevronRight size={14} /><strong>{getPageTitle(current)}</strong></div><div className="topbar-actions"><button className="command-trigger" onClick={() => setCommandOpen(true)}><Search size={14} /><span>Search anything</span><kbd>⌘ K</kbd></button><button className="icon-button" onClick={onThemeToggle}>{light ? <Moon size={16} /> : <Sun size={16} />}</button><button className="icon-button" onClick={() => toast("No new notifications")}><Bell size={16} /></button></div></header><div className="page-content">{children}</div></main>
    {commandOpen && <div className="command-overlay" onMouseDown={() => setCommandOpen(false)}><div className="command-dialog" onMouseDown={(e) => e.stopPropagation()}><div className="command-search"><Search size={16} /><input autoFocus placeholder="Search pages" value={searchTerm} onChange={(e) => setSearchTerm(e.target.value)} /><kbd>ESC</kbd></div><div className="command-results"><div className="command-label">Jump to</div>{navSections.flatMap((s) => s.items).filter((i) => i.label.toLowerCase().includes(searchTerm.toLowerCase())).map((item) => { const Icon = item.icon; return <button key={item.href} onClick={() => { setLocation(item.href); setCommandOpen(false); }}><Icon size={15} /><span>{item.label}</span><ChevronRight size={14} /></button>; })}</div></div></div>}
  </div>;
}

function getPageTitle(path: string) { return navSections.flatMap((s) => s.items).find((i) => i.href === path)?.label ?? "Dashboard"; }
function PageHeader({ eyebrow, title, description, action }: { eyebrow?: string; title: string; description?: string; action?: ReactNode }) { return <div className="page-header"><div><div className="eyebrow">{eyebrow ?? "Workspace"}</div><h1>{title}</h1>{description && <p>{description}</p>}</div>{action && <div className="page-header-action">{action}</div>}</div>; }
function MetricCard({ label, value, delta, direction, note, icon: Icon }: { label: string; value: string; delta: string; direction: "up" | "down"; note: string; icon: ElementType }) { return <div className="metric-card"><div className="metric-top"><span className="metric-label">{label}</span><Icon size={16} className="muted-icon" /></div><div className="metric-value">{value}</div><div className="metric-meta"><span className={direction === "up" ? "delta-up" : "delta-down"}>{direction === "up" ? <ArrowUpRight size={13} /> : <ArrowDownRight size={13} />}{delta}</span><span>{note}</span></div></div>; }
function EmptyState({ message, action, onAction }: { message: string; action: string; onAction: () => void }) { return <div className="empty-state"><span>{message}</span><button className="text-button" onClick={onAction}>{action} <ChevronRight size={14} /></button></div>; }

function Dashboard() { const [, setLocation] = useLocation(); const savedLeads = getSavedLeads(); const pipelineValue = savedLeads.reduce((sum, lead) => sum + lead.value, 0); return <><PageHeader eyebrow="Overview" title="Dashboard" description="A quiet view of the work that needs attention." action={<><button className="button secondary" onClick={() => toast(savedLeads.length ? "Export prepared" : "There is no data to export yet")}>Export CSV</button><button className="button primary" onClick={() => setLocation("/find-leads")}><Search size={15} /> Find leads</button></>} /><section className="metric-grid"><MetricCard label="Open pipeline" value={formatCurrency(pipelineValue)} delta={savedLeads.length ? `${savedLeads.length}` : "—"} direction="up" note={savedLeads.length ? "saved leads" : "no leads yet"} icon={Target} /><MetricCard label="Active proposals" value={String(savedLeads.filter((lead) => lead.stage === "Proposal").length)} delta="—" direction="up" note="saved proposals" icon={FileText} /><MetricCard label="Collected this month" value={formatCurrency(savedLeads.filter((lead) => lead.stage === "Won").reduce((sum, lead) => sum + lead.value, 0))} delta="—" direction="up" note="payments connect later" icon={Database} /><MetricCard label="Next action" value={`${savedLeads.filter((lead) => lead.stage === "New").length} leads`} delta="—" direction="down" note="in New" icon={Clock3} /></section><section className="dashboard-grid"><div className="panel pipeline-panel"><div className="panel-header"><div><h2>Pipeline</h2><p>Current lead distribution by stage.</p></div><button className="text-button" onClick={() => setLocation("/pipeline")}>View pipeline <ChevronRight size={14} /></button></div><div className="pipeline-total"><strong>{formatCurrency(pipelineValue)}</strong><span>saved value</span></div><div className={cn("pipeline-bar", !savedLeads.length && "pipeline-bar-empty")}>{savedLeads.length > 0 && stages.map((stage) => <span key={stage} className={`bar-${stageTone(stage)}`} style={{ width: `${Math.max(3, (savedLeads.filter((lead) => lead.stage === stage).length / savedLeads.length) * 100)}%` }} />)}</div>{savedLeads.length ? <div className="legend-grid">{stages.map((stage) => <div className="legend-item" key={stage}><span className={cn("legend-dot", `dot-${stageTone(stage)}`)} /><span>{stage}</span><strong>{savedLeads.filter((lead) => lead.stage === stage).length}</strong></div>)}</div> : <EmptyState message="No leads in the pipeline." action="Find leads" onAction={() => setLocation("/find-leads")} />}</div><div className="panel activity-panel"><div className="panel-header"><div><h2>Activity</h2><p>Latest changes across the workspace.</p></div><button className="icon-button" onClick={() => toast("Activity refreshed")}><RefreshCw size={15} /></button></div><EmptyState message={savedLeads.length ? "No activity logged yet." : "No activity yet."} action="Find leads" onAction={() => setLocation("/find-leads")} /></div></section><section className="panel table-panel"><div className="panel-header"><div><h2>Hot leads</h2><p>Qualified opportunities saved from your area searches.</p></div><button className="text-button" onClick={() => setLocation("/find-leads")}>Find leads <ChevronRight size={14} /></button></div>{savedLeads.length ? <LeadTable /> : <EmptyState message="No leads yet." action="Find leads" onAction={() => setLocation("/find-leads")} />}</section></>; }

function AddLeads() { const [tab, setTab] = useState("Single"); const [name, setName] = useState(""); const [url, setUrl] = useState(""); const [notes, setNotes] = useState(""); const tabs = ["Single", "Paste links", "Paste Maps details"]; const submit = (e: FormEvent) => { e.preventDefault(); toast.success(tab === "Single" ? "Lead added to New" : "Leads queued for review"); }; return <><PageHeader eyebrow="Lead intake" title="Add leads" description="Add a known business or bring in a prepared list." action={<span className="saved-indicator"><span className="live-dot" />Autosaved</span>} /><div className="tabs-line">{tabs.map((item) => <button key={item} className={cn("tab-button", tab === item && "tab-active")} onClick={() => setTab(item)}>{item}</button>)}</div><div className="split-layout"><form className="form-section" onSubmit={submit}><div className="form-block"><div className="form-label">{tab === "Single" ? "Business details" : "Source input"}</div>{tab === "Single" ? <><label>Business name<input value={name} onChange={(e) => setName(e.target.value)} placeholder="e.g. a Hyderabad design studio" required /></label><div className="form-row"><label>Website<input value={url} onChange={(e) => setUrl(e.target.value)} placeholder="company.in" /></label><label>Location<input defaultValue="Hyderabad, Telangana" /></label></div><div className="form-row"><label>Contact name<input placeholder="Optional" /></label><label>Contact email<input type="email" placeholder="name@company.in" /></label></div></> : <label>{tab === "Paste links" ? "Website links" : "Google Maps details"}<textarea value={url} onChange={(e) => setUrl(e.target.value)} placeholder={tab === "Paste links" ? "Paste one URL per line" : "Paste copied Maps results here"} rows={7} required /></label>}<label>Notes<textarea value={notes} onChange={(e) => setNotes(e.target.value)} placeholder="Context for the next step" rows={4} /></label></div><div className="form-footer"><button type="button" className="button secondary" onClick={() => { setName(""); setUrl(""); setNotes(""); }}>Clear</button><button type="submit" className="button primary"><Plus size={15} /> {tab === "Single" ? "Add lead" : "Queue import"}</button></div></form><aside className="helper-panel"><div className="helper-icon"><Target size={16} /></div><h2>Keep the first pass light</h2><p>Add the minimum context needed to create a lead. Audit and enrichment can fill in the gaps later.</p><div className="helper-list"><div><Check size={14} /> One record per line</div><div><Check size={14} /> Original source retained</div><div><Check size={14} /> Review before outreach</div></div><div className="helper-note"><CircleHelp size={14} /><span>Imports do not send outreach automatically.</span></div></aside></div></>; }

function FindLeads() { const [area, setArea] = useState("Secunderabad, Hyderabad"); const [query, setQuery] = useState("web design agencies"); const [radius, setRadius] = useState("5000"); const [map, setMap] = useState<google.maps.Map | null>(null); const [results, setResults] = useState<Lead[]>([]); const [searching, setSearching] = useState(false); const [searched, setSearched] = useState(false); const [error, setError] = useState(""); const [addedIds, setAddedIds] = useState<string[]>(() => getSavedLeads().map((lead) => lead.id)); const markers = useMemo(() => [] as google.maps.Marker[], []);
  const addLead = (lead: Lead) => { saveLead(lead); setAddedIds((ids) => ids.includes(lead.id) ? ids : [...ids, lead.id]); toast.success(`${lead.business} added to New`); }; const addAll = () => { results.forEach((lead) => saveLead(lead)); setAddedIds((ids) => Array.from(new Set([...ids, ...results.map((lead) => lead.id)]))); toast.success(`${results.length} businesses added to New`); }; const search = async () => { if (!map || !area.trim() || !query.trim()) return; setSearching(true); setError(""); setSearched(true); setResults([]); markers.forEach((marker) => marker.setMap(null)); try { const geocoder = new google.maps.Geocoder(); const geocode = await new Promise<google.maps.GeocoderResult>((resolve, reject) => geocoder.geocode({ address: `${area}, Telangana, India` }, (items, status) => items?.[0] && status === "OK" ? resolve(items[0]) : reject(new Error("Area not found")))); const center = geocode.geometry.location; map.setCenter(center); map.setZoom(13); const service = new google.maps.places.PlacesService(map); const found = await new Promise<google.maps.places.PlaceResult[]>((resolve, reject) => service.textSearch({ query: `${query} near ${area}, Hyderabad, Telangana, India`, location: center, radius: Number(radius) }, (items, status) => status === google.maps.places.PlacesServiceStatus.OK ? resolve(items ?? []) : reject(new Error(status === "ZERO_RESULTS" ? "No businesses found in this area." : "Places search failed.")))); const mapped = found.slice(0, 20).map((place, index) => { const position = place.geometry?.location; if (position) markers.push(new google.maps.Marker({ map, position, title: place.name })); return { id: place.place_id ?? `place-${index}`, business: place.name ?? "Unnamed business", niche: query, location: place.formatted_address ?? area, contact: "Not enriched", lastTouch: "Found just now", value: 0, initials: (place.name ?? "B").split(" ").slice(0, 2).map((word) => word[0]).join("").toUpperCase(), address: place.formatted_address, website: place.website, placeId: place.place_id, stage: "New" as Stage }; }); setResults(mapped); if (!mapped.length) setError("No businesses found in this area."); } catch (err) { setError(err instanceof Error ? err.message : "Search failed. Try a more specific area."); } finally { setSearching(false); } };
  return <><PageHeader eyebrow="Lead discovery" title="Find leads" description="Search Google Places for businesses around any area. Results stay here until you add them to the pipeline." action={<button className="button secondary" onClick={() => toast("Searches are not persisted until you add a lead")}>How it works</button>} /><div className="finder-layout"><section className="finder-panel"><div className="finder-controls"><label>Area<input value={area} onChange={(e) => setArea(e.target.value)} placeholder="Secunderabad, Hyderabad" /></label><label>Business type<input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="e.g. dental clinics" /></label><label>Radius<select value={radius} onChange={(e) => setRadius(e.target.value)}><option value="2000">2 km</option><option value="5000">5 km</option><option value="10000">10 km</option></select></label><button className="button primary finder-search" onClick={search} disabled={searching || !map}>{searching ? <><RefreshCw size={15} className="spin" /> Searching</> : <><Search size={15} /> Search area</>}</button></div><div className="map-frame"><MapView className="finder-map" initialCenter={{ lat: 17.4399, lng: 78.4983 }} initialZoom={12} onMapReady={setMap} /><div className="map-overlay-note"><span className="live-dot" /> Google Places search · no API key required</div></div></section><section className="finder-results"><div className="panel-header"><div><h2>{searched ? `${results.length} businesses found` : "Search results"}</h2><p>{searched ? `Around ${area}` : "Specify an area and business type to begin."}</p></div>{results.length > 0 && <button className="text-button" onClick={addAll} disabled={!results.length}>Add all <ChevronRight size={14} /></button>}</div>{error && <div className="finder-error">{error}</div>}{!searched && <div className="finder-empty"><Search size={18} /><strong>Find businesses around you</strong><span>Try “interior designers” in “Banjara Hills, Hyderabad”.</span></div>}{searched && !results.length && !error && <div className="finder-empty"><RefreshCw size={18} /><strong>Searching Places</strong><span>Results will appear here.</span></div>}{results.map((lead) => <div className="finder-result" key={lead.id}><div className="lead-avatar">{lead.initials}</div><div className="finder-result-copy"><strong>{lead.business}</strong><small>{lead.address ?? lead.location}</small>{lead.website && <small className="finder-website">{lead.website}</small>}</div><button className="row-action" disabled={addedIds.includes(lead.id)} onClick={() => addLead(lead)}>{addedIds.includes(lead.id) ? <Check size={16} /> : <Plus size={16} />}</button></div>)}</section></div></>; }

function LeadTable({ onSelect }: { onSelect?: (lead: Lead) => void }) { return <div className="table-wrap"><table className="data-table"><thead><tr><th>Business</th><th>Stage</th><th>Score</th><th>Contact</th><th>Last touch</th><th className="align-right">Value</th></tr></thead><tbody><tr><td colSpan={6}><EmptyState message="No leads yet." action="Find leads" onAction={() => toast("Open Find leads from the sidebar")} /></td></tr></tbody></table></div>; }
function PipelineView() { const [selected, setSelected] = useState<Lead | null>(null); const [view, setView] = useState<"board" | "table">("board"); const savedLeads = getSavedLeads(); return <><PageHeader eyebrow="Workspace" title="Pipeline" description="Move work forward with a clear view of every opportunity." action={<button className="button primary" onClick={() => toast("Use Find leads to discover businesses")}><Search size={15} /> Find leads</button>} /><div className="toolbar"><div className="toolbar-left"><button className="filter-button"><Filter size={14} /> All leads <ChevronDown size={13} /></button><button className="filter-button"><ListFilter size={14} /> Score: any</button><span className="toolbar-count">{savedLeads.length} leads</span></div><div className="view-toggle"><button className={cn(view === "board" && "view-active")} onClick={() => setView("board")}><KanbanSquare size={14} /> Board</button><button className={cn(view === "table" && "view-active")} onClick={() => setView("table")}><ListFilter size={14} /> Table</button></div></div>{view === "board" ? <div className="kanban-board">{stages.map((stage) => { const stageLeads = savedLeads.filter((lead) => lead.stage === stage); return <div className="kanban-column" key={stage}><div className="kanban-header"><span><span className={cn("legend-dot", `dot-${stageTone(stage)}`)} />{stage}</span><span className="column-count">{stageLeads.length}</span></div><div className="kanban-cards">{stageLeads.map((lead) => <button className="lead-card" key={lead.id} onClick={() => setSelected(lead)}><div className="lead-card-top"><span className="mono lead-id">{lead.id}</span><MoreHorizontal size={15} className="muted-icon" /></div><strong>{lead.business}</strong><span className="lead-card-meta">{lead.niche}</span><div className="lead-card-footer"><span className="muted-text">{lead.location}</span><span className="mono">{formatCurrency(lead.value)}</span></div></button>)}{!stageLeads.length && <div className="column-empty">No leads in this stage.</div>}</div><button className="column-add" onClick={() => toast("Use Find leads to add businesses")}><Plus size={14} /> Add lead</button></div>; })}</div> : <section className="panel table-panel">{savedLeads.length ? <LeadTable onSelect={setSelected} /> : <EmptyState message="No leads yet." action="Find leads" onAction={() => toast("Use Find leads from the sidebar")} />}</section>}{selected && <LeadDrawer lead={selected} onClose={() => setSelected(null)} />}</>; }

function LeadDrawer({ lead, onClose }: { lead: Lead; onClose: () => void }) { return <div className="drawer-overlay" onMouseDown={onClose}><aside className="lead-drawer" onMouseDown={(e) => e.stopPropagation()}><div className="drawer-header"><div><div className="eyebrow">{lead.id}</div><h2>{lead.business}</h2><p>{lead.niche} · {lead.location}</p></div><button className="icon-button" onClick={onClose}><X size={17} /></button></div><div className="drawer-body"><div className="drawer-empty"><div className="helper-icon"><FileCode2 size={16} /></div><h3>Lead details</h3><p>Add enrichment and outreach notes after saving this business to your pipeline.</p><button className="button secondary" onClick={() => toast("Lead added to New")}>Add to pipeline</button></div></div><div className="drawer-footer"><button className="button secondary" onClick={onClose}>Close</button><button className="button primary" onClick={() => toast("Lead moved to the next stage")}>Move to next stage <ChevronRight size={15} /></button></div></aside></div>; }

function AITools() { const [running, setRunning] = useState<string | null>(null); const operations = [{ title: "Website audit", description: "Review structure, clarity, and conversion friction.", count: "0 pending", last: "Never run", icon: Gauge }, { title: "Enrich leads", description: "Fill gaps in business, contact, and market context.", count: "0 pending", last: "Never run", icon: Sparkles }, { title: "Draft outreach", description: "Create a concise first message for qualified leads.", count: "0 pending", last: "Never run", icon: Send }, { title: "Generate proposal", description: "Turn a scoped opportunity into a reviewable proposal.", count: "0 pending", last: "Never run", icon: FileText }]; const run = (title: string) => { setRunning(title); setTimeout(() => { setRunning(null); toast("No leads available for this operation"); }, 900); }; return <><PageHeader eyebrow="Operations" title="AI tools" description="Run focused operations across saved leads. Nothing runs until there is data to process." /><div className="tools-grid">{operations.map((operation) => { const Icon = operation.icon; return <div className="tool-card" key={operation.title}><div className="tool-card-top"><div className="tool-icon"><Icon size={17} /></div><span className="tool-status">Ready</span></div><h2>{operation.title}</h2><p>{operation.description}</p><div className="tool-meta"><span>{operation.count}</span><span>Last run {operation.last}</span></div>{running === operation.title ? <div className="progress-row"><span className="progress-track"><span /></span><span className="mono">Working</span></div> : <button className="button secondary tool-run" onClick={() => run(operation.title)}>Run operation <ChevronRight size={14} /></button>}</div>; })}</div></>; }
function Revenue() { return <><PageHeader eyebrow="Operations" title="Revenue" description="Track Indian Rupee invoices and payments for your studio." action={<button className="button primary" onClick={() => toast("Add a lead first to create an invoice")}><Plus size={15} /> New invoice</button>} /><div className="metric-grid revenue-metrics"><MetricCard label="Collected" value={formatCurrency(0)} delta="—" direction="up" note="this month" icon={Check} /><MetricCard label="Outstanding" value={formatCurrency(0)} delta="—" direction="down" note="no invoices" icon={Clock3} /><MetricCard label="Clients" value="0" delta="—" direction="up" note="this quarter" icon={Users} /><MetricCard label="Avg. deal" value={formatCurrency(0)} delta="—" direction="up" note="no deals yet" icon={BriefcaseBusiness} /></div><section className="panel table-panel"><div className="panel-header"><div><h2>Invoices</h2><p>Currency: INR · GST-ready workflow · Hyderabad, Telangana.</p></div><button className="filter-button"><Filter size={14} /> All statuses <ChevronDown size={13} /></button></div><EmptyState message="No invoices yet." action="Add a lead" onAction={() => toast("Use Find leads to create a client record")} /></section></>; }
function Logs() { return <><PageHeader eyebrow="System" title="Logs" description="Trace every operation, provider response, and failure." /><div className="toolbar"><div className="toolbar-left"><div className="search-input"><Search size={14} /><input placeholder="Filter operations" /></div><button className="filter-button"><Filter size={14} /> Status: all <ChevronDown size={13} /></button><span className="toolbar-count">0 events</span></div><button className="button secondary" onClick={() => toast("There are no logs to export yet")}>Export logs</button></div><section className="panel table-panel"><EmptyState message="No operations have run yet." action="Find leads" onAction={() => toast("Use Find leads from the sidebar")} /></section></>; }
function Settings() { const [saved, setSaved] = useState(false); return <><PageHeader eyebrow="System" title="Settings" description="Control models, Indian payment defaults, and workspace data." action={saved ? <span className="saved-indicator"><Check size={14} />Saved</span> : undefined} /><div className="settings-layout"><div className="settings-nav"><button className="settings-nav-active">General</button><button>API keys</button><button>Model</button><button>Payments</button><button>Data</button><button className="settings-danger">Danger zone</button></div><div className="settings-content"><SettingSection title="Workspace" description="Business details used in documents and payment records."><div className="setting-row"><div><strong>Business location</strong><span>Default jurisdiction for this workspace.</span></div><select defaultValue="Hyderabad, Telangana"><option>Hyderabad, Telangana</option><option>Secunderabad, Telangana</option></select></div><div className="setting-row"><div><strong>Currency</strong><span>Used for pipeline values and invoices.</span></div><select defaultValue="INR — Indian Rupee"><option>INR — Indian Rupee</option></select></div></SettingSection><SettingSection title="Payments" description="India-first payment defaults. Connect a provider before sending payment requests."><div className="setting-row"><div><strong>Payment provider</strong><span>Razorpay supports UPI, cards, net banking, and Indian settlement.</span></div><select defaultValue="Razorpay"><option>Razorpay</option><option>Manual bank transfer</option></select></div><div className="setting-row"><div><strong>Tax handling</strong><span>GST details can be added before an invoice is issued.</span></div><select defaultValue="GST enabled"><option>GST enabled</option><option>GST not registered</option></select></div><div className="key-row"><div><KeyRound size={16} /><span><strong>Razorpay connection</strong><small>Not connected</small></span></div><button className="text-button" onClick={() => toast("Connect Razorpay when credentials are ready")}>Connect</button></div></SettingSection><SettingSection title="API keys" description="Keys are encrypted and only shown once. Replace access without leaving the page."><div className="key-row"><div><KeyRound size={16} /><span><strong>Google Gemini</strong><small>Not configured</small></span></div><button className="text-button" onClick={() => toast("Gemini key replacement opened")}>Add key</button></div><div className="key-row"><div><KeyRound size={16} /><span><strong>Groq fallback</strong><small>Not configured</small></span></div><button className="text-button" onClick={() => toast("Groq key replacement opened")}>Add key</button></div></SettingSection><SettingSection title="Danger zone" description="Actions in this section cannot be undone."><div className="danger-row"><div><strong>Delete workspace data</strong><span>Remove all saved leads, activity, and generated outputs.</span></div><button className="button destructive" onClick={() => toast("There is no saved data to delete")}>Delete data</button></div></SettingSection><div className="settings-footer"><button className="button primary" onClick={() => { setSaved(true); toast.success("Settings saved"); }}>Save changes</button></div></div></div></>; }
function SettingSection({ title, description, children }: { title: string; description: string; children: ReactNode }) { return <section className="settings-section"><div className="settings-section-head"><h2>{title}</h2><p>{description}</p></div>{children}</section>; }

export default function Home() { const [location] = useLocation(); const [light, setLight] = useState(false); const page = useMemo(() => { switch (location) { case "/find-leads": return <FindLeads />; case "/add-leads": return <AddLeads />; case "/pipeline": return <PipelineView />; case "/ai-tools": return <AITools />; case "/revenue": return <Revenue />; case "/logs": return <Logs />; case "/settings": return <Settings />; default: return <Dashboard />; } }, [location]); return <Shell light={light} onThemeToggle={() => { setLight((v) => !v); document.documentElement.classList.toggle("light"); }}>{page}</Shell>; }
export { StatusBadge, MetricCard, SettingSection };
