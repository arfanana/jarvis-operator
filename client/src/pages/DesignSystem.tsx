import { useState } from "react";
import { Link, useLocation } from "wouter";
import { ArrowLeft, Check, ChevronDown, Copy, Download, Info, Loader2, Search, X } from "lucide-react";
import { toast } from "sonner";
import { StatusBadge } from "./Home";

type DemoStatus = "New" | "Contacted" | "Interested" | "Proposal" | "Won" | "Lost";

function ButtonDemo({ label, variant = "primary", disabled = false }: { label: string; variant?: "primary" | "secondary" | "ghost" | "destructive"; disabled?: boolean }) {
  return <button className={`button ${variant}`} disabled={disabled} onClick={() => toast(`${label} clicked`)}>{label}</button>;
}

export default function DesignSystem() {
  const [, setLocation] = useLocation();
  const [status, setStatus] = useState<DemoStatus>("Interested");
  return <div className="design-page"><header className="design-header"><div><Link href="/" className="back-link"><ArrowLeft size={15} /> Back to app</Link><div className="eyebrow">Jarvis UI</div><h1>Design system</h1><p>A review surface for the components, states, and tokens that keep the workspace consistent.</p></div><button className="button secondary" onClick={() => toast("Token reference copied")}><Copy size={14} /> Copy tokens</button></header><div className="design-grid"><section className="design-section"><div className="design-section-header"><div><span className="micro-label">01</span><h2>Buttons</h2><p>Small, direct actions with one accent signal.</p></div></div><div className="demo-row"><ButtonDemo label="Primary action" /><ButtonDemo label="Secondary" variant="secondary" /><ButtonDemo label="Ghost" variant="ghost" /><ButtonDemo label="Destructive" variant="destructive" /><ButtonDemo label="Disabled" disabled /></div></section><section className="design-section"><div className="design-section-header"><div><span className="micro-label">02</span><h2>Inputs</h2><p>Labels stay above fields. Focus remains visible.</p></div></div><div className="demo-form"><label>Search leads<div className="search-input focus-demo"><Search size={14} /><input defaultValue="Northline" /></div></label><label>Stage<select value={status} onChange={(event) => setStatus(event.target.value as DemoStatus)}><option>New</option><option>Contacted</option><option>Interested</option><option>Proposal</option><option>Won</option><option>Lost</option></select></label><label>Notes<textarea placeholder="Add context for the next step" rows={3} /></label></div></section><section className="design-section"><div className="design-section-header"><div><span className="micro-label">03</span><h2>Statuses</h2><p>Status is a signal, not decoration.</p></div></div><div className="demo-row status-row"><StatusBadge stage="New" /><StatusBadge stage="Contacted" /><StatusBadge stage="Interested" /><StatusBadge stage="Proposal" /><StatusBadge stage="Won" /><StatusBadge stage="Lost" /></div></section><section className="design-section"><div className="design-section-header"><div><span className="micro-label">04</span><h2>Table row</h2><p>Dense enough for work, quiet enough to scan.</p></div></div><div className="panel design-table"><table className="data-table"><thead><tr><th>Business</th><th>Status</th><th>Owner</th><th className="align-right">Value</th></tr></thead><tbody><tr><td><strong>Northline Architecture</strong><small>Architecture studio · Portland, OR</small></td><td><StatusBadge stage={status} /></td><td>Marin Cole</td><td className="align-right mono">$6,800</td></tr><tr className="selected-row"><td><strong>Field & Form</strong><small>Interior design · Austin, TX</small></td><td><StatusBadge stage="Proposal" /></td><td>Marin Cole</td><td className="align-right mono">$5,200</td></tr></tbody></table></div></section><section className="design-section"><div className="design-section-header"><div><span className="micro-label">05</span><h2>Feedback states</h2><p>Loading, success, and errors stay inline.</p></div></div><div className="feedback-grid"><div className="feedback-card"><span className="feedback-label">Loading</span><div className="skeleton-line long" /><div className="skeleton-line short" /><span className="loading-label"><Loader2 size={13} className="spin" /> Loading records</span></div><div className="feedback-card success-state"><span className="feedback-label">Success</span><div className="feedback-message"><Check size={15} /><span>Import queued for review.</span></div></div><div className="feedback-card error-state"><span className="feedback-label">Error</span><div className="feedback-message"><X size={15} /><span>Provider timeout. <button className="text-button">Retry</button></span></div></div><div className="feedback-card info-state"><span className="feedback-label">Info</span><div className="feedback-message"><Info size={15} /><span>Nothing is sent automatically.</span></div></div></div></section><section className="design-section"><div className="design-section-header"><div><span className="micro-label">06</span><h2>Modal anatomy</h2><p>A centered, reversible decision surface.</p></div></div><div className="modal-demo"><div className="modal-demo-header"><div><span className="eyebrow">Confirmation</span><h3>Move lead forward?</h3></div><button className="icon-button"><X size={16} /></button></div><p>Northline Architecture will move from Interested to Proposal. No outreach will be sent.</p><div className="modal-demo-footer"><ButtonDemo label="Cancel" variant="secondary" /><ButtonDemo label="Move to proposal" /></div></div></section></div><footer className="design-footer"><button className="text-button" onClick={() => setLocation("/")}><ArrowLeft size={14} /> Return to dashboard</button><span className="mono">jarvis-ui / v0.1</span></footer></div>;
}
